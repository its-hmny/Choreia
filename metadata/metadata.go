package metadata

type PackageMetadata struct{}

type FunctionMetadata struct{}

func ExtractAndSave(inputPath, outputPath string, verbose bool) (int, error) {
	return 0, nil
}

func Extract(inputPath string, verbose bool) ([]PackageMetadata, error) {
	return []PackageMetadata{}, nil
}
